# Each time `rails new` is ran, rails will pull the options from this file (located in the home dir) and apply them as arguments.
# See https://github.com/rails/rails/blob/main/railties/lib/rails/generators/rails/app/app_generator.rb for details on --minimal.
--minimal
--skip-bundle
--skip-keeps
--skip-test
